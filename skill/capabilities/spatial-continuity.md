# Spatial Continuity Capability

Status: `FORMALIZED_PRODUCTION_CANDIDATE`.

Load as dominant only when connected Scene state is the main problem; otherwise use as a narrow axis/direction/path/boundary modifier.

- State the minimum evidence-backed regions, relative direction, connection, screen direction, and entry/exit.
- Preserve a stable action/dialogue axis; if crossing is necessary, use a readable neutral move or re-establish relation.
- Track position, facing, carried hand, contact target, and action phase only where later readability depends on them.
- At a connected Scene boundary, pair prior `continuity_end_state` with following `initial_state`.
- Mirror, reflection, POV, and deliberate anomaly declare the normal relation and exact violation.
- Never infer a Shot delta from Scene end state.

Does not own unsupported coordinates, production approval, or a complete 3D solution.
