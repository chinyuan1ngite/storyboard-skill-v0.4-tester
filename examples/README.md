# Examples

这些示例只展示如何组织短剧本、请求和输出结构，不是 benchmark、黄金样例、固定镜数要求或 Schema 权威。

- `simple-dialogue/`：两人一句问答。
- `action/`：明确起点、路径和结果。
- `suspense/`：控制 reveal 时机。
- `fantasy-or-supernatural/`：违反现实物理但遵守世界内部规则。

每个 `expected-output-shape.md` 只列应看到的结构和检查点，不提供唯一“标准导演答案”。
